# OpenShift

This directory will contain reproducible installation inputs, Operator
subscriptions, cluster configuration, and Argo CD applications for the Single
Node OpenShift lab.

Pull secrets, kubeconfigs, private keys, and decrypted application secrets are
excluded from Git. Cluster and base-domain names must be settled before install
because they are costly to change later.
